# /etc/kayveedb/kayveedb.conf

# snapshot is a point in time snapshot of the current state of the database.
# logpath is where the journal logs are kept for recovery

snapshot = "/var/lib/kayveedb/snapshot.db"
logpath = "/var/lib/kayveedb/operation.log"
